name: test
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run a multi-line script
      run: |
        TARGET=$(podman run -t --rm --volume=$PWD:/gl ghcr.io/gardenlinux/builder:3ab2bb52bc46bb200c761369c087e9413d1ce0ac /builder/parse_features --feature-dir /gl/features --features kvm,lima,_curl cname_base)
        echo target $TARGET
